services:
  - type: web
    name: domscan-bot
    env: python
    region: frankfurt
    buildCommand: ""
    startCommand: "python bot.py"
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
